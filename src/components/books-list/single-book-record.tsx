import React from 'react';


interface IProps {

}

const SingleBookRecord = (props: IProps) => {

  return (
    <section >
      single book record
    </section>
  );
};

export default SingleBookRecord;
